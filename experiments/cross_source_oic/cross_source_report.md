# object_inside_container — One Task, Four Worlds

_Generated by `scripts/build_cross_source_report.py`. Every verdict is recomputed live from committed inputs through the frozen verifier core (`csg.matcher.match` + `csg.rollout_extract.extract_robot_csg`); `csg/` is byte-frozen. Reproduces from a clean clone with no MuJoCo / RLBench / cv2 installed._


**The task.** Did the object end up inside the container, having been put there (a real outside→inside transition), not merely born inside?


**The claim.** The same semantic task, instantiated per source as a target card and judged by the same frozen verifier core, PASSes genuine successes, rejects non-successes (0 false PASSes in every world), stays leakage-clean, and reports physical validity honestly (true only for the internal sim, where physics was actually re-checked; null for the three external worlds).


## Cross-source scoreboard

| World | Verifier path | Successes (certified / total) | UNCERTAIN | hard-FAIL (false-neg) | Non-success false-PASS | Leakage | physicalValidity |
|---|---|---|---|---|---|---|---|
| MuJoCo internal sim | `extract_robot_csg` | **1/1** | 0 | 0 | **0/0** | clean | **true** (physics re-checked) |
| RLBench external sim | `verify_external_rollout` | **9/9** | 0 | 0 | **0/0** | clean | null (physics-unverified) |
| Sony/iPhone real camera | `pilots.real_camera.verify_episode` | **27/38** | 5 | 6 | **0/40** | clean | null (physics-unverified) |
| RH20T real robot video | `verify_external_rollout` | **1/1** | 0 | 0 | **0/1** | clean | null (physics-unverified) |

_Across all four worlds: **38** successes certified, **5** UNCERTAIN (fail-closed on weak evidence), **6** hard-FAIL false-negatives (a known real-camera occlusion limit, below), and **0** false-PASSes on **41** non-success clips. Leakage-clean in every world: **True**._


_"Certified" = the STRUCTURED tier PASSes (a real outside→inside put-in: `relation_event` near-start OR `placed_from_outside` far-start). "false-PASS" = a non-success clip the structured tier wrongly certifies. The weak `terminal_only` tier ("did it merely END inside?") is intentionally easier — it is what a born-inside episode passes, which is exactly why the structured tier exists. The UNCERTAIN column counts SUCCESS-clip abstentions only; non-success clips that are UNCERTAIN are counted as correctly rejected (they do not certify), not as false-PASSes._


## Per world


### MuJoCo internal sim

- **source:** Genuine MuJoCo backend solver run of put_cube_in_tray (committed fixture; experiments/cross_source_oic/mujoco_fixture/).
- **verifier path:** `extract_robot_csg -> csg.matcher.match -> leakage_report (== csg.benchmark.run_one)`
- **physical validity:** **true** (physics re-checked)
- the genuine MuJoCo solver rollout PASSes `terminal_only` AND the structured tier, with **physicalValidity = true** (5/5 applicable physics checks pass — articulation_limits is N/A, no articulated joints). It also PASSes its native full pick-place gold target `human_put_cube_in_tray` (a SUPERSET adding contact / carry / temporal-order probes: passed=True).
- internal acceptance corpus (committed `gold_tests/put_cube_in_tray/` robot CSGs): the frozen matcher PASSes the success and FAILs all **4** sabotaged variants (missing-contact / removed-at-end / wrong-order / wrong-relation) — every verdict matches the committed `expected.json` (True).

### RLBench external sim

- **source:** 9 live RLBench PutItemInDrawer demos recorded on CoppeliaSim (pilots/rlbench/fixtures/live_runpod_20260616_put_item/).
- **verifier path:** `verify_external_rollout (leakage door -> extract_robot_csg -> match -> leakage_report)`
- **physical validity:** null (physics-unverified)
- all **9** live demos PASS `terminal_only`; the structured tier certifies every one (**3** near-start via `relation_event`, **6** far-start via `placed_from_outside`).
- in-data discrimination (a success-only world): each demo FAILs the WRONG-precondition structured tier (**9/9** wrong-tier rejections) — the verifier is reading the demonstrated start state, not rubber-stamping.

### Sony/iPhone real camera

- **source:** 78 committed real-camera clips (Sony 45° + iPhone top), object_inside_container_v0 (datasets/sony_object_inside_container_v0/).
- **verifier path:** `pilots.real_camera.verify_episode (fail-closed UNCERTAIN gate -> verify_external_rollout)`
- **physical validity:** null (physics-unverified)
- **27/38** genuine successes certified; **5** UNCERTAIN (fail-closed on occlusion/low-confidence — correct abstentions, not wrong FAILs); **6** hard-FAIL false-negatives where brief hand/tag obstruction corrupts the terminal relation without tripping the evidence gate (an honest known limitation, not part of the claim).
- **0/40** structured false-PASSes on non-success clips. The **3** non-success clips that pass the WEAK `terminal_only` tier are object-already-inside controls (born-inside / inside-to-inside) — they correctly FAIL the structured tier (no outside→inside transition), which is the whole point of having it.
- evidence-quality thresholds (relaxed 30fps): `{'max_consecutive_missing': 30, 'max_dropout_frac': 0.35}` (the same `EVIDENCE_THRESHOLDS` the ingest pipeline + baseline experiment use).

### RH20T real robot video

- **source:** RH20T public dataset, task_0017 'put the pen into the pen holder' (1 real positive + 1 derived negative; datasets/rh20t_object_inside_container_v0/).
- **verifier path:** `verify_external_rollout (leakage door -> extract_robot_csg -> match -> leakage_report)`
- **physical validity:** null (physics-unverified)
- the real positive episode (pen→holder) PASSes `terminal_only` AND `relation_event` (non-vacuous); the derived negative (pen placed beside the holder) FAILs both. 0 false-PASS.
- source-blind: the rollout carries only a neutral `RH20T` label + a one-way episode hash + the archive digest — no task id, description, or scene path reaches the verifier.

## Same task, not the same file — target-card equivalence

Each world is verified against its OWN target card (different `graphId`, object labels, geometry provenance, captions). They are NOT one shared file. But every card reduces to the SAME verifier-enforced semantic core per tier, with all ids canonicalised to roles and authoring-only fields stripped:

| Tier | Sources compared | Enforced cores identical? |
|---|---|---|
| `terminal_only` | real_camera, rh20t, rlbench | **True** (1 distinct signature) |
| `relation_event` | real_camera, rh20t, rlbench | **True** (1 distinct signature) |
| `placed_from_outside` | real_camera, rlbench | **True** (1 distinct signature) |

- the tiers are nested: `terminal_only` enforces only the INSIDE goal (True); `relation_event` adds the authored NEAR→INSIDE endpoints + the containment event (True); the INSIDE goal is unchanged across all tiers (True).
- `placed_from_outside` differs from `relation_event` ONLY in the initial authored relation endpoint (r0: NEAR vs FAR_FROM) — the single fact `initial_state` reads to tell a near-start put-in from a far-start one (True).
- the MuJoCo internal world is judged against its native gold pick-place target (`human_put_cube_in_tray`), a SUPERSET; its INSIDE goal core matches the shared core (True) — the internal world is judged at a STRONGER tier, never a weaker one. Full proof: `target_equivalence.json`.


## Honest caveats

- **Two verifier paths, one core.** The three external worlds go through `verify_external_rollout` (the leakage door + the frozen core); MuJoCo goes through the frozen core directly (`extract_robot_csg → match → leakage_report`, == `csg.benchmark.run_one`) because its legitimate internal `objectIdMap` would (correctly) be rejected by the external door. The matcher and relation extractor are the identical frozen functions in both paths.
- **physicalValidity is honest, not uniform.** `true` ONLY for MuJoCo (physics genuinely re-checked at capture); `null` for the three external worlds (a recorded trace cannot be physics-revalidated). A `null` PASS is labelled *physics-unverified*, never *valid*.
- **Failure data is uneven across worlds.** Sony (40 non-success clips) and RH20T (1 derived negative) carry explicit failures; the two sim worlds are success-only in their committed live data, so their discrimination is shown via the wrong-tier rejection (RLBench) and the committed sabotage corpus (MuJoCo).
- **Sony hard-FAIL false-negatives are real.** A handful of genuine successes hard-FAIL when brief obstruction corrupts the terminal relation without tripping the evidence gate. They are reported, not hidden; they are a perception limit of marker-only 3A capture, not a verifier claim.

